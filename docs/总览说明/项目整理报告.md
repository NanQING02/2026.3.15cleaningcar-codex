# 项目整理报告

## 本次整理目标

- 保留现有功能不变，只整理运行链路和目录结构
- 明确主运行路径只保留当前真正使用的模型和代码
- 把测试文件、遗留模型、未启用模块移出主目录
- 明确哪些目录是当前运行，哪些目录是后续开发或历史保留

## 当前主运行链路

- 检测模型：`models/detection/best.rknn`
- 车牌双模型：`models/plate/plate_detect.rknn` + `models/plate/plate_rec_color.rknn`
- 默认配置目录：`configs/`
- 主程序入口：`run_zone_detect.py`
- 主代码目录：`cleaningcar/`
- Web 管理目录：`web/`

## 本轮新增整理

### 检测后处理解耦

- `cleaningcar/fp_detect.py`
  - 现在兼容 `6` 输出和 `9` 输出模型解析
- `video.fp_output_mode`
  - 控制使用 `6` 模式还是 `9` 模式
- 默认值为 `6`
  - 即模型给 `9` 输出时，也只用 `box + class`

### 配置同步

- `configs/config.json`
- `configs/config_绕行.json`

都已加入：

```json
"fp_output_mode": "6"
```

## 当前目录语义

- `cleaningcar/`
  - 当前实际运行的检测、跟踪、事件与视频处理代码
- `models/`
  - 当前实际运行模型
- `configs/`
  - 当前实际运行配置文件
- `future_modules/`
  - 未启用模块、历史实现、后续开发保留内容
- `docs/`
  - 项目说明和运行文档
- `tools/`
  - 当前不保留运行必需脚本，历史测试脚本已清理
