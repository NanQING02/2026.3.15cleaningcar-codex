# 后置项记录

## 1. `events/` 与日志清理

当前项目已经具备截图与单车视频清理，但还没有覆盖以下目录：

- `events/<配置名>/`
- `logs/inference/`
- `web_server_*.log`

后续可评估：

- 是否按天数清理
- 是否按数量清理
- 是否需要保留最近 1 次完整验收日志集

## 2. 配置样板进一步清单化

建议后续补齐：

- “最小可运行配置”样板
- “板端验收配置”样板
- 对 `storage.*`、`system.*`、`logic.*` 高频项做交付清单

## 3. 服务化细项

当前已经收口成 4 个脚本，并提供 `cleaningcar-web.service`。若后续还要继续细化，可再评估：

- 是否需要“只卸载 systemd 服务”的独立入口
- 是否需要 systemd 环境变量模板化
- 是否需要不同端口 / 不同配置的多实例模板

## 4. 本轮明确不做

以下内容本轮不做，避免继续扩大改动面：

- 额外 HTTP 接口扩展
- 模型结构调整
- 车牌前后处理算法重写
- 事件规则重写
