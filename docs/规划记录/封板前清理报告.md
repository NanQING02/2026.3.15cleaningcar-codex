# 封板前清理报告

本次清理只做“不会改变主功能”的收口动作，不改模型逻辑，不改事件规则，不改 HTTP 路由。

## 已完成

### 1. 补齐仓库忽略规则

补充了以下通用忽略项：

- `__pycache__/`
- `*.pyc`
- `*.pyo`
- `*.pyd`
- `.pytest_cache/`
- `.mypy_cache/`
- `.ruff_cache/`

目的：

- 防止再次把 Python 编译缓存、测试缓存带进仓库
- 降低 `git status` 被运行缓存污染的概率

### 2. 增加板端验收文档

新增：

- `docs/部署验收/板端验收清单.md`

内容覆盖：

- Web 启动
- 推理启动
- 心跳检查
- 启动信号检查
- 手动截图检查
- 自启恢复检查
- `fp_output_mode=6/9` 对比检查

## 本次不动的内容

以下内容本次故意不动：

- `models/` 下当前正式模型
- `configs/` 下当前正式配置
- `future_modules/` 下历史保留模块
- 已整理好的主代码解耦结构

原因：

- 当前目标是封板前清理，不是再次改业务逻辑
- 先把版本稳定性验证跑完，再决定是否继续改功能

## 建议你现在马上做的事

1. 按 `docs/部署验收/板端验收清单.md` 在 RKNN 板端跑完整验收
2. 确认正式默认值到底保留 `fp_output_mode=6` 还是切到 `9`
3. 验收通过后再做一次版本提交，形成可回退基线
