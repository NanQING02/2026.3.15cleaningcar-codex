# 部署验收目录

这个目录只放板端部署和验收相关文档。

## 文件说明

- `环境目录说明.md`
  - `packages/`、`venv-gst/`、四个脚本、`systemd` 模板的用途
- `运行路径说明.md`
  - 运行期会读写哪些目录、模型、日志、截图和控制文件
- `板端验收清单.md`
  - 从安装环境到启动 Web、启动推理、检查心跳与截图的完整验收步骤

## 建议使用方式

- 上板前先看 `环境目录说明.md`
- 需要排查路径问题时看 `运行路径说明.md`
- 正式验收按 `板端验收清单.md` 逐项执行
