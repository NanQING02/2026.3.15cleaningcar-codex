# future_modules/legacy_models 目录说明

这个目录存放已退役或历史保留的模型文件。

## 当前文件

- `best_legacy_pre_fp.rknn`
  - 历史检测模型
- `lprnet_legacy_single_model.rknn`
  - 历史单模型车牌识别模型

## 使用约束

- 当前正式运行不要直接使用这里的模型
- 正式模型统一放在 `models/`
